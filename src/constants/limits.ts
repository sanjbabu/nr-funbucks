/**
 * The number of filters to limit the configuration to.
 */
export const FB_FILTER_LIMIT = 200;

/**
 * The number of inputs to limit the configuration to.
 */
export const FB_INPUT_LIMIT = 200;

/**
 * The number of parsers to limit the configuration to.
 */
export const FB_PARSER_LIMIT = 200;
